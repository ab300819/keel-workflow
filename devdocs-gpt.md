# DevDocs 开发流程审查报告

> 范围：本仓库 DevDocs 工作流（`requirements → system-design → test-cases → dev-tasks → sync`）及辅助技能（`retrofit/feature/insights/onboard`）。
>
> 结论摘要：整体框架是“可追溯的文档驱动开发闭环”，设计思路正确；但存在一致性与可操作性问题，会在真实项目里造成“文档维护成本高、容易漂移、编号混乱、路径选择不清”的风险。

---

## 1) 流程现状（真实主路径）

### 正向路径 A（需求明确）

`/devdocs-requirements → /devdocs-system-design → /devdocs-test-cases → /devdocs-dev-tasks → 开发实现 → /devdocs-sync`

### 逆向路径 B（探索/已有代码）

先写代码/原型 → `/devdocs-retrofit` 逆向生成 DevDocs → 人工审查补齐 →（补测试）→ 回到 `dev-tasks/sync`

### 辅助闭环

- `/devdocs-feature`：在已有 DevDocs 上“增量追加”
- `/devdocs-sync`：检查/更新文档进度、归档已完成任务
- `/devdocs-insights`：把调研/审查建议转成需求入口
- `/devdocs-onboard`：交接上下文

整体逻辑是通的，且 TAR（dev tasks）与追溯矩阵（test cases）是亮点。

---

## 2) 关键问题与风险点（按影响优先级排序）

### P0：术语/命令不一致，会直接导致流程跑偏

- 发现 `refactor/SKILL.md` 仍引用 `/devdocs-test-plan`，但实际主流程是 `/devdocs-test-cases`。
  - 这属于“硬断链”：用户/Agent按旧名调用会失败或走错分支。
- 建议：全仓统一为 `devdocs-test-cases`（包括 README、refactor、任何模板/示例）。

### P0：编号规范在示例层面不统一，容易引发重复/冲突

- 主规范多处强调 **3 位**（`F-001 / US-001 / AC-001`），但 README 与部分示例里出现 **2 位**（`US-01 / AC-01`），`devdocs-gemini.md` 也混用（如 `AC-01` vs `UT-020`）。
- `T` 任务目前常用 `T-01`（2 位），与其它前缀不同；这可以接受，但必须明确“例外规则”，否则迁移/校验会复杂。
- 更大的结构性问题：**INS（洞察）与 T（任务）没有进入全局编号表**（`CLAUDE.md`/`README` 的“统一编号体系”缺失），但它们实际是工作流的重要节点。
  - 结果：新用户看全局表会误以为没有 INS/T，追溯链不完整。

### P1：`/devdocs-feature` 试图“一次性更新四份核心文档”，可操作性差

- feature skill 的目标是对 `01/02/03/04` 做增量追加，但一次会话里同时维护：
  - 编号延续（防重复）
  - 影响分析（系统设计）
  - 测试补齐（test-cases）
  - 任务拆分（dev-tasks）
- 这对人/Agent 的注意力都是高负荷，极易出现：
  - 漏更新某一份文档
  - 编号重复/断号
  - 追溯矩阵没补齐（AC 新增但测试表没加）
- 本质：`devdocs-feature` 更像“编排器”，但现在更像“全能写手”。

### P1：`/devdocs-sync` 目前偏“检查员”，缺少“低风险自动吸收”能力

- sync 已经提出“同步应该双向（文档→代码、代码→文档）”，但落地仍以“生成偏差报告 + 人工确认”为主。
- 缺的能力（会显著降低维护成本）：
  - 从测试结果/测试文件存在性 **自动回填** 文档状态（例如追溯矩阵✅、任务测试结果字段）
  - 从代码结构中发现“已实现但文档未记录”的增量内容，并提供“可一键吸收”的最小变更建议（不是改代码，是补文档）
- 仓库里已有方向性建议（如 `devdocs-gemini.md` 提到 `--fix/--absorb`）。

### P2：路径边界存在重叠，用户不易选对入口

- “已有代码但无文档/文档不全/在做增量迭代”在 `retrofit` 与 `feature` 的触发面上有交叉。
- README 虽然有“路径选择表”，但对以下灰区不够明确：
  - “已有部分 DevDocs，但过时”到底优先 sync 还是 retrofit？
  - “只有需求文档，没有测试/任务”算正向补齐还是 retrofit？
  - “很小的变更（小功能/配置/微调）”走全链条成本过高。

---

## 3) 可落地的优化建议（优先级路线图）

### 3.1 立刻该做的（P0）

1. **统一命名：彻底消灭 `devdocs-test-plan`**
   - 全仓搜索替换，保证 refactor/README/示例一致。
2. **把 INS 与 T 纳入“全局编号体系”**
   - 在 `README.md` 与 `CLAUDE.md` 的编号表里加入：
     - `INS-XXX`（洞察）
     - `T-XX`（任务，明确是否 2 位为特例）
3. **明确编号位数规则**
   - 推荐：除 `T` 外一律 `XXX`；如果 `T` 保持 `XX`，写成“例外：任务两位编号”。

### 3.2 降低维护成本的（P1）

4. **将 `/devdocs-feature` 明确定位为“编排器/向导”**
   - 目标不是“一次写完四份文档”，而是：
     - Step 1：只追加 requirements（产生新 F/US/AC）
     - Step 2：如有影响 → system-design 增量更新（影响分析/兼容性）
     - Step 3：补 test-cases（追溯矩阵补齐）
     - Step 4：补 dev-tasks（TAR + 依赖图）
   - 每步确认后再进入下一步，降低漏项。
5. **增强 `/devdocs-sync` 的“吸收模式”**
   - 设计上可以是 `--absorb`/`--fix`：
     - 自动更新：任务勾选状态、测试通过状态、追溯矩阵状态
     - 仍然保留“更新前必须询问用户确认”的总原则（现有约束是正确的）。

### 3.3 体验与边界（P2）

6. **增加一个“轻量路径”（devdocs-lite）或在 README 增加“轻量规则”**
   - 例如：小变更只要求：
     - 在 `01-requirements.md` 追加 AC（或 INS→F）
     - 在 `04-dev-tasks.md` 增加 1-3 个任务
     - `/devdocs-sync check` 验证一致性
   - 目的是避免“为改一行配置也走四文档全套”。

---

## 4) 建议优先确认的关键决策

你希望 **`T` 任务编号** 继续保持 `T-01`（两位）作为特例，还是也升级为 `T-001` 以完全统一？

- 统一成 `T-001`：校验/排序/工具化更简单，长期更一致
- 保持 `T-01`：短小直观，但需要文档里明确“唯一例外”，并且工具要兼容两位

如果你确认偏好，我可以基于该选择给出“最小改动的规范修订清单”（只改规范/示例，不动整体结构）。
