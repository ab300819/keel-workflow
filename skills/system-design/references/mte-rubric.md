# MTE 评审标准（MTE Rubric）

系统设计必须遵循 MTE 原则。

## MTE 三维度

| 原则 | 说明 | 检查点 |
|------|------|--------|
| **Maintainability** | 可维护性 | 模块职责单一、依赖清晰、易于理解和修改 |
| **Testability** | 可测试性 | 核心逻辑可单元测试、依赖可 Mock、边界清晰 |
| **Extensibility** | 可扩展性 | 预留扩展点、接口抽象、开闭原则 |

## 可维护性检查

- [ ] 每个模块职责单一，不超过一个变化原因
- [ ] 依赖方向：外层依赖内层，内层不依赖外层
- [ ] 接口优于实现：依赖抽象，不依赖具体实现

## 可测试性检查

- [ ] 核心业务逻辑可单元测试（无外部依赖或依赖可 Mock）
- [ ] 服务层与基础设施层解耦
- [ ] 边界清晰，可独立验证

## 可扩展性检查

- [ ] 预留合理扩展点，但不为假设需求设计
- [ ] 使用的设计模式解决实际问题（非为使用而使用）
- [ ] 优先选择简单方案，复杂方案需说明理由

## 避免过度设计

- [ ] 不为"未来可能"的需求设计
- [ ] 不创建只有一个实现的接口（除非为了可测试性）
- [ ] 不过早抽象，等到有 3 个以上相似场景再抽象
- [ ] 配置优于硬编码，但不为所有内容添加配置

## 引用方

- `ms-system-design`：设计文档生成后自检
- `ms-verify`：设计符合度审查时参照
