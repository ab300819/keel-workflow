# 设计方案草案模板（初始 + 增量）

> 配合 `/ms-system-design` 使用。在分析完成、写入文档前展示草案并使用 AskUserQuestion 等待用户确认。

## `--fast` 边界自查块（必答，嵌入草案顶部）

`--fast` 模式下 L1~L4 分层交互合并为一次展示，**以下块必须完整填写**（不允许空着或整体"跳过"），否则视为质量门退化：

```markdown
## --fast 边界自查

### L1~L4 边界校验

| 层级 | 产出章节 | 边界检查 | 结果 |
|------|---------|---------|------|
| L1 架构 | §2 + §1 | 无模块级细节泄漏到架构图 | ✅/⚠️ |
| L2 模块 | §4 | 职责单一；"依赖接口引用"列格式合规 | ✅/⚠️ |
| L3 接口行为 | §5 | 无越界反例（7 类，见 design-boundary-guide.md） | ✅/⚠️ |
| L4 数据模型 | §8 | 仅写调用方不可见细节；可观察约束已在 §5 错误契约 | ✅/⚠️ |

### 越界反例自查（决策 4 清单，L3 完成时强制）
- [ ] 接口只描述"做什么"（签名+契约）
- [ ] 未引入内部数据结构（Map/Queue/State）
- [ ] 无伪代码块
- [ ] 字段只说明语义和约束
- [ ] 无具体时序策略数值（超时/重试/退避）
- [ ] §4 "依赖接口引用"列均为 `I*(§5.x)` 或"无"

### 遗留 ⏳ 项汇总
（列出所有标 `⏳` 的占位；若无则填"无"）
```

## 初始设计方案草案

```markdown
## 设计方案草案

### 技术选型
- 语言/框架：<选择> — <理由>
- 数据库：<选择> — <理由>
- 其他关键依赖：<列表>

### 架构方向
- 架构风格：<分层/微服务/Serverless/...>
- 核心设计决策：<列表>

### 已有实现核对
| 需求关键词 | 搜索结果 | 状态 | 证据位置 |
|-----------|---------|------|---------|
| <接口/实体名> | <找到的类/表/方法> | 已有/扩展/新增 | <文件:行号> |

### 模块划分（对外接口引用 + 依赖接口引用，面向接口原则）
| 模块 | 状态 | 对外接口引用 | 依赖接口引用 | 职责 | 关联功能点 |
|------|------|------------|------------|------|-----------|
| ... | 已有/扩展/新增 | `I*(§5.x)` | `I*(§5.x)` 或"无" | <单一职责> | F-XXX |

### 关键接口概要（L3 层）
- <接口名> [已有/扩展/新增]: <行为契约摘要——前置/后置/错误>

### 关键数据模型（L4 层）
| 表/实体 | 状态 | 说明 | 关联功能点 |
|---------|------|------|-----------|
| ... | 已有/扩展/新增 | <一句话> | F-XXX |

### 风险与权衡
- <取舍点1>: 选择 A 而非 B，因为 ...

### 对抗性设计审查
（详见 `references/design-review.md`；按复杂度分级触发 SOLID+LoD 原则校验）

| 视角 | 挑战问题 | 回应 | 结论 |
|------|---------|------|------|
| 产品 | <用户价值是否达成？> | <回应> | 接受/驳回 |
| 工程 | <技术债是否可控？> | <回应> | 接受/驳回 |
| 测试 | <关键路径是否可测试？> | <回应> | 接受/驳回 |

### 原则校验（按复杂度三态强制填写 + 适用下限）

| 原则 | 状态 | 理由（违反时必填） |
|------|------|----------------|
| SRP | ✅/—/⚠️ | — 或 ADR 编号 |
| OCP | ... | ... |
| LSP | ... | ... |
| LoD | ... | ... |
| ISP | ... | ... |
| DIP | ... | ... |

**适用下限校验**（⛔，防"全填不适用"绕过）：
- 单模块：SRP/LoD/DIP 中至少 **2 项** `✅ 适用`
- 低-中复杂度：4 原则中至少 **3 项** `✅ 适用`
- 高复杂度：6 原则中至少 **5 项** `✅ 适用`
- 未达下限 → 审查结论"待修正"，不放行

> 以上是设计方案，是否可以开始生成文档？如需调整请说明。
```

## 增量设计方案草案

```markdown
## 变更方案草案

### 变更来源
- F-XXX / INS-XXX: <描述>

### 影响范围
| 影响对象 | 影响类型 | 向后兼容 |
|----------|----------|----------|
| ... | 新增/修改/删除 | ✅/❌ |

### 变更策略
- <具体变更动作列表>

### 破坏性变更处理
- <如有，列出迁移方案>

### 修订清单（必填，≤10 行）

| § | 章节 | 修改方式 | 一句话描述 |
|---|------|---------|----------|
| §4 | AuthModule | 改写 | 新增 resetPassword 组件 |
| §5.1 | IUserService.createUser | 局部调整 | 错误契约补充 RATE_LIMITED |

动作枚举：新增 / 改写 / 局部调整 / 重构
若确无正文修改（仅决策补记）→ 登记"仅追加 ADR 的理由"；否则不得进入追加 ADR 步骤。

### 对抗性设计审查

| 视角 | 挑战问题 | 回应 | 结论 |
|------|---------|------|------|
| 产品 | <变更是否影响已有用户价值？> | <回应> | 接受/驳回 |
| 工程 | <与现有架构的兼容性？> | <回应> | 接受/驳回 |
| 测试 | <变更是否引入不可测试路径？> | <回应> | 接受/驳回 |

### 原则校验（同初始设计要求）

（表格同上，三态强制填写）

> 以上是变更方案，是否可以开始更新文档？如需调整请说明。
```
