# DevDocs 工作流优化建议 (针对个人开发者)

## 1. 概述

DevDocs 是一套逻辑严密、基于追溯体系 (F/US/AC -> UT/IT/E2E) 的开发工作流。虽然它在保证工程质量和逻辑完整性方面表现卓越，但在**个人开发者 (Solo Developer)** 的实际场景中，存在管理成本过高、流程过于线性、文档维护负担重等问题。

本文档旨在针对上述痛点提出优化建议，以平衡“开发效率”与“工程质量”。

---

## 2. 核心问题分析

### 2.1 流程沉重与任务规模不匹配 (Overhead vs. Scale)
- **现象**：对于耗时短的小型功能或 Bug 修复，生成四个独立文档 (01-04) 的时间成本远超编码本身。
- **痛点**：繁琐的文档初始化降低了开发者使用规范流程的意愿。

### 2.2 文档维护债 (Synchronization Debt)
- **现象**：开发阶段发现设计缺陷时，需同步修改 01、02、03、04 多个文件。
- **痛点**：多点维护导致文档与代码快速脱节，文档最终沦为陈旧的摆设。

### 2.3 测试门槛过于理想化 (Idealized Testing Standards)
- **现象**：强制 80% 覆盖率和变异测试得分在 MVP (最小可行性产品) 阶段过于苛刻。
- **痛点**：过度追求指标可能导致开发者为了“凑数”而编写低质量测试。

### 2.4 线性流程的局限 (Linearity vs. Iteration)
- **现象**：DevDocs 默认是自上而下的瀑布模式，缺乏“边做边改”或“先写代码再补文档”的逆向支持。

---

## 3. 优化建议与解决方案

### 3.1 引入分层工作流 (Tiered Workflow)

建议根据任务复杂度自动切换模式：

| 任务级别 | 定义 | 流程要求 | 文档形式 |
| :--- | :--- | :--- | :--- |
| **Simple** | < 1小时，无架构改动 | 直接执行 + Commit 规范 | 无需 DevDocs 文档 |
| **Medium** | 1-4小时，局部逻辑改动 | **DevDocs-Lite** | 合并为一个 `feature-spec.md` |
| **Complex** | > 4小时，涉及架构/多模块 | **Standard DevDocs** | 完整 01-04 独立文档 |

### 3.2 建立 "DevDocs-Lite" 规范
将需求、设计、测试用例和任务拆分整合到一个文件中，减少文件切换和格式冗余：
- **[F-001] 功能点**
- **[AC-001] 验收标准** (包含测试方法)
- **[T-01] 开发任务** (关联 AC)

### 3.3 文档自动化与回填 (Auto-Retrofit)
- **代码驱动文档**：强化 `/devdocs-retrofit` 能力，支持根据已实现的函数注释和测试代码，自动回传更新需求与设计文档。
- **实时同步**：当 T-XX 任务在实现中发生变更时，AI 应主动提示是否更新 01/02 文档，而非等待用户手动修改。

### 3.4 阶梯式质量指标 (Progressive Quality Gates)
- **项目初期**：重点保证 P0 流程的 E2E 测试，行覆盖率要求放低至 40%-50%。
- **核心/稳定期**：对 Service/Domain 层开启 80% 强制覆盖率和变异测试约束。

---

## 4. 改进后的 DevDocs 推荐路径 (Solo 版)

### 路径 A：正向快轨 (New Feature)
1. 执行 `/devdocs-requirements` -> 生成**合并版**文档。
2. 直接进入开发任务，AI 在编码中动态维护文档状态。
3. 提交前执行一次 `/code-quality` 审查。

### 路径 B：探索后补 (Exploratory First)
1. 开发者直接编写核心逻辑原型。
2. 执行 `/devdocs-retrofit` -> 逆向提取 F/US/AC，生成标准化文档以沉淀资产。
3. 在文档基础上补充测试用例，完成工程化加固。

---

## 5. 结论

个人开发的 DevDocs 应从“瀑布式约束”转向“声明式辅助”。核心目标不应是“写完所有文档”，而是**“确保每一项变更都有据可查 (Traceable)”**。通过合并小任务文档、引入逆向生成能力，可以让 DevDocs 成为加速开发的引擎而非负担。
